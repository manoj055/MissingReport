<nav class="navbar navbar-expand-sm  sticky-top bg-dark nav-dark">
      <a class="navbar-brand"><%= link_to "MISSING PERSON REPORT",people_path %> </a>
      <button class="navbar-toggler bg-light" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item active">
            <a class="nav-link_home">
              <span class="sr-only"> <%= link_to "Home",people_path, class: "btn" %> </span>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link_about" href="#"><%= link_to "About",about_index_path, class: "btn" %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link_service" href="#"><%= link_to "Services",services_path, class: "btn" %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link_contact" href="#"><%= link_to "Contact",contact_index_path, class: "btn" %></a>
          </li>
          <li class="nav-item">
            <div class="nav-link_report">
              <%= link_to "Report",new_person_path, class: "btn" %>
            </div>
          </li>
          <% if !user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link_sign_out" href="#"><%= link_to('Login', new_user_session_path,class: "btn btn-success")  %></a>
          </li>
          <% else %>
          <li class="nav-item">
          <a class="nav-link_sign_out" href="#"><%= link_to('Logout', destroy_user_session_path, method: :delete,class: "btn btn-danger")  %>  </a>
          </li>
          <% end %>
          
            <div class="uname text-center">
            <% if user_signed_in? %>
            <a href="#" class="btn btn-info">Welcome: <%= current_user.uname %></a>
            <% end %>
          </div>
        </ul>
      </div>
   </nav>
